{
  "name": "@rksp/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:fuzz": "vitest run test/schemas.fuzz.test.ts",
    "test:watch": "vitest",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\" --ignore-pattern \"src/**/*.d.ts\""
  },
  "dependencies": {
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "fast-check": "^4.8.0",
    "vitest": "^3.1.2"
  }
}
